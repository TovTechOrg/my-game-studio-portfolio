# Prompt 1

```
Create a Flappy Bird style game in index.html under /games/05-flappy-bird. The player is a procedural white ghost (canvas drawing). Gravity pulls it down constantly. Obstacles are glowing green pipes moving left
```

# Prompt 2

```
Make the game mobile-responsive. Bind both the 'Spacebar' key AND the 'Screen Tap' (touchstart) to the Jump action. Prevent scrolling when tapping
``` 

# Prompt 3

```
the game is unplayable because after a tap it immediately sends the player to the ground
``` 

# Prompt 4

```
Add sound effects using JavaScript AudioContext (synthesized, no external files). 1. A short high-pitch 'beep' when jumping. 2. A low-pitch 'buzz' when crashing. 3. A 'ding' when passing a pipe
``` 

# Prompt 5

```
the sounds should only play as long as the game is not over
``` 

# Prompt 6

```
There is a problem where after clicking “Try again” the button is clickable even though it's supposed to be hidden and unclickable
``` 

# Prompt 7

```
Add a 'Game Over' overlay centered on screen with a 'Play Again' button. Also, save the 'High Score' to LocalStorage so it remembers my best run
```

# Prompt 8

```
in @index.html change "Click or Space or Tap to Float” to adapt to the device running on. If running on the desktop, it should say "Click or Space to Float". if it runs on mobile, it should say instead “Tap to Float"
```
